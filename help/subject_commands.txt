Part 1 : 

#connect to ssh
vagrant ssh lciulloS
vagrant ssh lciulloSW

#Check if k3s is installed 

#From the master 
vagrant ssh lciulloS
sudo kubectl get nodes -o wide

#Check the master mod 
vagrant ssh lciulloS


#From the agent 
vagrant ssh lciulloSW
ip a show eth1

#Check the agent mod (check k3s)
If you see an active k3s service, it means that this machine is in master (controller) mode.
vagrant ssh lciulloSW
sudo systemctl status k3s

#Lastes version of k3s
